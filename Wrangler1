wr.s3.to_parquet(
    df=df,
    path="s3://your-target-bucket/final-dataset/",
    dataset=True,
    partition_cols=["dt"],  # partitioned storage
    mode="overwrite",
    database="your_target_database",
    table="your_parquet_table"
)
